[package]
name = "startpage"
version = "1.0.0"
edition = "2021"

[dependencies]
wee_alloc = "0.4"
sycamore = "0.9"
getrandom = { version = "0.4", features = ["wasm_js"] }
rand = "0.10"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
